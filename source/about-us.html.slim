---
title: About Us | The Research Faculty
---

section.hero style=background_image(image_path(data.pages.about.hero.image))
  .caption
    .row
      h1 = data.pages.about.hero.title

section.homepage-story
  .container
    .row
      .story-content
        = markdown data.pages.about.story

section.homepage-team.team
  .container
    h2
      | The Team
    .team-items
      - blog.articles.each do | article |
        = partial 'partials/team/team_item', locals: {article: article}
